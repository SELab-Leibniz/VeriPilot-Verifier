# Requirements / Design -> Coding Checkpoint 边审阅标准

1. 浏览器旅程必须覆盖 Requirements 的 R07 与 R09，并与 Design 的 UI 绑定和测试契约一致。
2. 任何筛选、编辑、完成或删除行为失败，都不能以模型单元测试通过抵消。
3. Coding PASS 必须绑定当前浏览器证据，不得使用计划、旧 revision 或矛盾的内层状态。
4. 不增加产品范围，不降低上游可访问性和关键旅程要求。

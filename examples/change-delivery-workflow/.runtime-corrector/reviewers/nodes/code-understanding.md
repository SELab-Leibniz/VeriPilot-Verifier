# 代码理解节点审查

核对报告与实际源码和上游文档：

1. 文件、函数、调用链和数据流必须能从相关源码中得到支持，不得臆造不存在的结构。
2. 影响点和风险必须覆盖需求报告中的每个 `REQ-`。
3. 若相关文件中存在 UX 设计稿，代码理解对输入、反馈和状态的描述必须与其一致；UX 不存在时不得因此报错。
4. 只描述现状与变更影响，不提前把某个实现方案写成既定事实。

优先报告事实错误、遗漏的影响面和跨文档冲突。

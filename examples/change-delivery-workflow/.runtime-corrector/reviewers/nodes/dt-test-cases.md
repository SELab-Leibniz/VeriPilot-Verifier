# DT 测试节点审查

判断开发者测试设计是否：

1. 将每个 `REQ-` 和关键人工场景映射为可自动化的断言。
2. 选择合理测试层级，明确隔离边界、Mock、测试数据和副作用验证。
3. 覆盖正常、错误、边界和回归路径，尤其验证不应发生的文件写入等副作用。
4. 不绑定未经方案确认的实现细节，也不遗漏方案中的风险与回滚条件。

报告无法自动化、断言不明确、覆盖缺口或与上游语义冲突的问题。

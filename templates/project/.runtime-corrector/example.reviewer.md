# Example Document 审阅标准

仅在 `config.yaml` 中设置 `review.enabled: true` 后使用。

1. 检查文档结论是否有项目事实或上游产物支持。
2. 检查是否遗漏目标、范围、约束和可验证结果。
3. 每个问题必须引用具体文件证据；证据不足时标记为待确认，不得补造事实。
4. 只允许对当前目标 artifact 提出候选修改，相关文件保持只读。

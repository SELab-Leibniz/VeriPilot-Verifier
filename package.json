{
  "name": "runtime-corrector",
  "version": "1.3.3",
  "private": true,
  "type": "module",
  "description": "Explainable, project-configured runtime correction for artifact workflows.",
  "bin": {
    "runtime-corrector": "./scripts/cli.mjs"
  },
  "scripts": {
    "check": "node ./scripts/cli.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  }
}
